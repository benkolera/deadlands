name: common
version: "0.1"

dependencies:
  - base
  - lens
  - text
  - nat
  - containers

ghc-options: -Wall

library:
  source-dirs: src
  exposed-modules:
    - Common.Api
    - Common.DiceSet

tests:
  tests:
    main: Main.hs
    source-dirs:
      - src
      - tests
    dependencies:
      - tasty
      - tasty-discover
      - tasty-hunit
